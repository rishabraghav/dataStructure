#include <iostream>

#include<cstring>

using namespace std;

void removeConsecutiveDuplicates(char *input) {
    int size = strlen(input);
    
}

int main() {
    char s[100000];
    cin >> s;
    removeConsecutiveDuplicates(s);
    cout << s << endl;
}